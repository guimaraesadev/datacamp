-- in addition to using aggregate functions
-- you can perform basic arithmetic with symbols like +, -, *, and /.

-- so, for example, this gives a result of 12:

SELECT (4 * 3);

-- However, the following gives a result of 1:

SELECT (4 / 3);

SELECT (10 / 3) AS result

-- OUTPUT: result 3 

